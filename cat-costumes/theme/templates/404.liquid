<div class="wrap">
  <div class="empty">
    <p style="font-size:3rem;margin:0">🙀</p>
    <h1>{{ 'general.404.title' | t }}</h1>
    <p class="muted">{{ 'general.404.body' | t }}</p>
    <a class="btn" href="{{ routes.all_products_collection_url }}">{{ 'general.404.link' | t }}</a>
  </div>
</div>
